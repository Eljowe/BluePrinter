---
id: "0035"
title: "Library curation: setlists, favourites and bulk edits"
status: done
blocked_by: []
---

# Library curation: setlists, favourites and bulk edits

## Problem

The library is a flat, single-item grid with colour tags. There is no way to group
takes into a set for a performance, mark favourites to prioritise them, or act on
several takes at once. As the library grows this becomes the main friction.

## Current behaviour

`SnippetList.jsx` offers frontend-only search/sort/key-filter/tag-chip filtering and
caps the grid at 10 cards + "Show more". `SnippetCard.jsx` operations (rename,
colour, gain, export, reveal) are per-item only. Snippets persist WAV + JSON in the
library folder.

## Required change

1. **Favourites/rating** on a snippet, persisted in the JSON sidecar and shipped in
   the snapshot, plus a filter/toggle in the toolbar.
2. **Named setlists** (ordered lists of snippet ids) stored in the properties file:
   create/rename/reorder/add/remove, with a setlist view or filter.
3. **Multi-select** in the grid with bulk actions: set tag colour, add to setlist,
   export, delete (with confirmation).
4. Keep filtering/sorting frontend-only; do not move search into C++.
5. Define behaviour when a setlist entry is deleted or its file is missing.

## Acceptance criteria

- Rating/favourite survives restart and a sidecar export round-trip.
- Setlists persist across restart and reordering is stable.
- Bulk tag / delete / add-to-setlist act on the whole selection, with confirmation or
  undo for destructive actions.
- The grid stays responsive on a large library.

## Docs

`AGENTS.md` (library/snippets), `README.md`, `webui-bridge` skill.

## Files

`Source/SnippetLibrary.h/.cpp`, `Source/PluginProcessor.h/.cpp` (user state),
`Source/WebViewEditor.h/.cpp`, `WebUI/src/components/SnippetList.jsx`,
`WebUI/src/components/SnippetCard.jsx`, `WebUI/src/bridge.js`.

## Out of scope

Cloud sync; auto-generated smart playlists; drag-and-drop between setlists.

## Comments

> *This was generated by AI during triage.*

2026-09-13 — Triage started. Decisions recorded so far:

- **Favourite model:** a single boolean favourite (star) per snippet, persisted
  in the JSON sidecar and shipped in the snapshot (not a 1–5 rating).
- **Setlist storage:** named, ordered lists of snippet ids in the properties
  file next to `tagNames` (stable setlist ids so reordering survives), read on
  the non-chain user-state restore.
- **Dependency:** setlists need the same restore path that ticket 0041 restores
  (`restoreUserState` is currently dead code, so `tagNames` — and any new
  setlists key — are not loaded at launch). Land 0041 first.

Still open (needs a decision before this can move to `ready-for-agent`): whether
to split this into three tickets (favourites / setlists / bulk edits), and the
documented behaviour when a setlist entry's snippet is deleted or its file is
missing.

2026-09-13 — **Phase 1 (favourites) implemented.** `Snippet.favourite` (bool,
default false) persists as `favourite` in the JSON sidecar (`SnippetLibrary::
updateFavourite`, read/write, unit-tested incl. the disk round-trip);
`frontendSetSnippetFavourite { id, favourite }` → `setSnippetFavourite` (immediate
persist + `libraryChanged`); `snippetToVar` ships it; `SnippetCard` shows a star
toggle in its header and `SnippetList` gains a frontend-only **Favourites** filter.
Verified: WebUI build green, Debug standalone compiles, `ctest` green.
Remaining phases: **setlists** (properties-file, needs the same restore path as
`tagNames`) and **bulk select + actions** (frontend, with a new add-to-setlist
event).

2026-09-13 — **Phase 2 (named setlists) implemented.** New pure module
`Source/SetlistStore.h/.cpp` (+ `Tests/test_SetlistStore.cpp`, 6 tests) owns
ordered, duplicate-free snippet-id lists with stable ids and JSON
(in)serialisation; the processor persists them under the `setlists` properties
key with the same debounced write as `tagNames`, loads them in
`restoreUserState`, and prunes ids that no longer exist on folder load and on
snippet delete. Events `frontendCreateSetlist`/`frontendRenameSetlist`/
`frontendDeleteSetlist`/`frontendAddSnippetToSetlist`/`frontendRemoveSnippet-
FromSetlist`/`frontendSetSetlistOrder`; the snippets payload ships
`setlists: [{id, name, ids}]`. UI: a setlist filter (its order is authoritative
in that view) + a Setlists manage popover in `SnippetList`, and a Setlists chip
row (with up/down reorder in a filtered view) in `SnippetCard`. Verified: WebUI
build green, Debug standalone compiles, `ctest` green. Remaining phase: **bulk
select + actions**.

2026-09-13 — **Phase 3 (bulk select + actions) implemented; ticket done.** A
toolbar **Select** toggle adds a checkbox to each card and a bulk bar (select
all shown, set colour on the selection, add the selection to a setlist, delete
with a two-step confirm). New one-snapshot bulk backend methods/events:
`frontendSetSnippetsColor { ids, color }`, `frontendAddSnippetsToSetlist
{ setlistId, ids }`, `frontendDeleteSnippets { ids }` (persist once, single
`libraryChanged`); the delete path prunes setlists once. The selection is pruned
when snippets disappear. All three acceptance items (bulk tag / add-to-setlist /
delete with confirmation) are met; the grid stays frontend-only. Verified: WebUI
build green, Debug standalone compiles, `ctest` green.
